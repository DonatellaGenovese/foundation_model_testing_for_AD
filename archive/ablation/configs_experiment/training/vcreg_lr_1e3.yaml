# @package _global_
# vcreg lr=0.001 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vcreg
  - override /paths: ablation_lr_cern
  - _self_

tags: ["ablation", "lr", "vcreg", "d128", "lr_1e3"]

model:
  optimizer:
    lr: 0.001

logger:
  mlflow:
    experiment_name: "ablation_lr"
    run_name: "vcreg_lr_1e3"
