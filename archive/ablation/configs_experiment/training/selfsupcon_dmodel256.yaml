# @package _global_
# selfsupcon d_model=256 ablation
defaults:
  - ablation/training/backbone_selfsupcon
  - _self_

tags: ["ablation", "training", "selfsupcon", "dmodel256", "100k"]
model:
  d_model: 256
  n_heads: 8
  d_ff: 1024
  projection_dim: 128
  hidden_projection_dim: 512
logger:
  mlflow:
    run_name: "selfsupcon_dmodel256"
