# @package _global_
# vcreg d_model=512 ablation
defaults:
  - ablation/training/backbone_vcreg
  - _self_

tags: ["ablation", "training", "vcreg", "dmodel512", "100k"]
model:
  d_model: 512
  n_heads: 8
  d_ff: 2048

logger:
  mlflow:
    run_name: "vcreg_dmodel512"
