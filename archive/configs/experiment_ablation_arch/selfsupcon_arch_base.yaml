# @package _global_
# SELFSUPCON backbone architecture ablation — best d=128 configuration.
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_backbone_cern
  - _self_

tags: ["ablation", "backbone", "selfsupcon", "d128", "100k"]

model:
  d_model: 128
  n_heads: 8
  num_layers: 8
  d_ff: 1024
  dropout: 0.23

logger:
  mlflow:
    experiment_name: "ablation_backbone"
    run_name: "selfsupcon_arch_base"
