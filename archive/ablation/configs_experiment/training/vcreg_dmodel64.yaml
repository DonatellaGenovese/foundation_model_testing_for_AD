# @package _global_
# vcreg d_model=64 ablation
defaults:
  - ablation/training/backbone_vcreg
  - _self_

tags: ["ablation", "training", "vcreg", "dmodel64", "100k"]
model:
  d_model: 64
  n_heads: 8
  d_ff: 256

logger:
  mlflow:
    run_name: "vcreg_dmodel64"
