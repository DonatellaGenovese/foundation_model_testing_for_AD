# @package _global_
# VICReg dropout=0.2 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vicreg
  - override /paths: ablation_dropout_cern
  - _self_

tags: ["ablation", "dropout", "vicreg", "d128", "dropout_20"]

model:
  dropout: 0.2

logger:
  mlflow:
    experiment_name: "ablation_dropout"
    run_name: "vicreg_dropout_20"
