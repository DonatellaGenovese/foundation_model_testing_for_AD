# @package _global_
# vicreg d_model=512 ablation — random particle augmentation
defaults:
  - ablation/training/backbone_vicreg
  - _self_

tags: ["ablation", "training", "vicreg", "dmodel512", "100k"]
model:
  d_model: 512
  n_heads: 8
  d_ff: 2048
  projection_dim: 256
  hidden_projection_dim: 1024

logger:
  mlflow:
    run_name: "vicreg_dmodel512"
