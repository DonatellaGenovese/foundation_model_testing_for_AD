# @package _global_
# aug_supcon weight_decay=0 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_aug_supcon
  - override /paths: ablation_wd_cern
  - _self_

tags: ["ablation", "wd", "aug_supcon", "d128", "wd_0"]

model:
  optimizer:
    weight_decay: 0.0

logger:
  mlflow:
    experiment_name: "ablation_wd"
    run_name: "aug_supcon_wd_0"
