# @package _global_
# VICREG backbone ablation — d=128, dropout=0.2.
defaults:
  - ablation/training/vicreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vicreg", "d128", "dropout_20"]

model:
  dropout: 0.2

logger:
  mlflow:
    run_name: "vicreg_arch_dropout_20"
