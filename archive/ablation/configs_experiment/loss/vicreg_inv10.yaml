# @package _global_
# VICReg loss ablation — inv_weight=10
defaults:
  - ablation/loss/backbone_vicreg_physics
  - _self_

tags: ["ablation", "loss", "vicreg", "inv10"]
model:
  inv_weight: 10.0
logger:
  mlflow:
    run_name: "vicreg_inv10"
