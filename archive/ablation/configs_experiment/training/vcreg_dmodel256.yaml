# @package _global_
# vcreg d_model=256 ablation
defaults:
  - ablation/training/backbone_vcreg
  - _self_

tags: ["ablation", "training", "vcreg", "dmodel256", "100k"]
model:
  d_model: 256
  n_heads: 8
  d_ff: 1024

logger:
  mlflow:
    run_name: "vcreg_dmodel256"
