# @package _global_
# aug_supcon d_model=256 ablation
defaults:
  - ablation/training/backbone_aug_supcon
  - _self_

tags: ["ablation", "training", "aug_supcon", "dmodel256", "100k"]
model:
  d_model: 256
  n_heads: 8
  d_ff: 1024
  projection_dim: 128
  hidden_projection_dim: 512
logger:
  mlflow:
    run_name: "aug_supcon_dmodel256"
