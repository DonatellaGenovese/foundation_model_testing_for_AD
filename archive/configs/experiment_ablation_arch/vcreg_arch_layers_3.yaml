# @package _global_
# VCREG backbone ablation — d=128, layers=3.
defaults:
  - ablation/training/vcreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vcreg", "d128", "layers_3"]

model:
  num_layers: 3

logger:
  mlflow:
    run_name: "vcreg_arch_layers_3"
