# @package _global_
# vcreg d_model=128 ablation
defaults:
  - ablation/training/backbone_vcreg
  - _self_

tags: ["ablation", "training", "vcreg", "dmodel128", "100k"]
model:
  d_model: 128
  n_heads: 8
  d_ff: 512

logger:
  mlflow:
    run_name: "vcreg_dmodel128"
