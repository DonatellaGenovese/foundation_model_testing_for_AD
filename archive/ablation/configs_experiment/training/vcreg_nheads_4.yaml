# @package _global_
defaults:
  - ablation/training/backbone_vcreg
  - override /paths: ablation_nheads_cern
  - _self_

tags: ["ablation", "nheads", "vcreg", "d128", "nheads_4"]

model:
  n_heads: 4

logger:
  mlflow:
    experiment_name: "ablation_nheads"
    run_name: "vcreg_nheads_4"
