# @package _global_
# selfsupcon weight_decay=0.01 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_wd_cern
  - _self_

tags: ["ablation", "wd", "selfsupcon", "d128", "wd_1e2"]

model:
  optimizer:
    weight_decay: 0.01

logger:
  mlflow:
    experiment_name: "ablation_wd"
    run_name: "selfsupcon_wd_1e2"
