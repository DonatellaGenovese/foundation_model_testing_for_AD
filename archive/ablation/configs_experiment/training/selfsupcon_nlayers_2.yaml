# @package _global_
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_nlayers_cern
  - _self_

tags: ["ablation", "nlayers", "selfsupcon", "d128", "nlayers_2"]

model:
  num_layers: 2

logger:
  mlflow:
    experiment_name: "ablation_nlayers"
    run_name: "selfsupcon_nlayers_2"
