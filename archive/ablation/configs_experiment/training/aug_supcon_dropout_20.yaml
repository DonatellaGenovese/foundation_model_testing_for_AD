# @package _global_
# AugSupCon dropout=0.2 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_aug_supcon
  - override /paths: ablation_dropout_cern
  - _self_

tags: ["ablation", "dropout", "aug_supcon", "d128", "dropout_20"]

model:
  dropout: 0.2

logger:
  mlflow:
    experiment_name: "ablation_dropout"
    run_name: "aug_supcon_dropout_20"
