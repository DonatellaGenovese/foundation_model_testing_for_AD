# @package _global_
# SELFSUPCON backbone ablation — d=128, layers=6.
defaults:
  - ablation/training/selfsupcon_arch_base
  - _self_

tags: ["ablation", "backbone", "selfsupcon", "d128", "layers_6"]

model:
  num_layers: 6

logger:
  mlflow:
    run_name: "selfsupcon_arch_layers_6"
