# @package _global_
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_nlayers_cern
  - _self_

tags: ["ablation", "nlayers", "selfsupcon", "d128", "nlayers_4"]

model:
  num_layers: 4

logger:
  mlflow:
    experiment_name: "ablation_nlayers"
    run_name: "selfsupcon_nlayers_4"
