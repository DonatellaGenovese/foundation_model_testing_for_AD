# @package _global_
# vicreg d_model=64 ablation — random particle augmentation
defaults:
  - ablation/training/backbone_vicreg
  - _self_

tags: ["ablation", "training", "vicreg", "dmodel64", "100k"]
model:
  d_model: 64
  n_heads: 8
  d_ff: 256
  projection_dim: 32
  hidden_projection_dim: 128

logger:
  mlflow:
    run_name: "vicreg_dmodel64"
