# @package _global_
# VCReg loss ablation — var_weight=50 (baseline: 25)
defaults:
  - ablation/loss/backbone_vcreg
  - _self_

tags: ["ablation", "loss", "vcreg", "var50"]
model:
  var_weight: 50.0
  cov_weight: 1.0
