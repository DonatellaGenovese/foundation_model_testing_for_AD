# @package _global_
# aug_supcon batch_size=1024 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_aug_supcon
  - override /paths: ablation_bs_cern
  - _self_

tags: ["ablation", "bs", "aug_supcon", "d128", "bs_1024"]

data:
  batch_size: 1024

logger:
  mlflow:
    experiment_name: "ablation_bs"
    run_name: "aug_supcon_bs_1024"
