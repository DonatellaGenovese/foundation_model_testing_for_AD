# @package _global_
# VCREG backbone ablation — d=128, layers=2.
defaults:
  - ablation/training/vcreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vcreg", "d128", "layers_2"]

model:
  num_layers: 2

logger:
  mlflow:
    run_name: "vcreg_arch_layers_2"
