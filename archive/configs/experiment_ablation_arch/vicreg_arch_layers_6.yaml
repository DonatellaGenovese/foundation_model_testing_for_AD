# @package _global_
# VICREG backbone ablation — d=128, layers=6.
defaults:
  - ablation/training/vicreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vicreg", "d128", "layers_6"]

model:
  num_layers: 6

logger:
  mlflow:
    run_name: "vicreg_arch_layers_6"
