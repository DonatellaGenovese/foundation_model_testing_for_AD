# @package _global_
# AUG_SUPCON backbone ablation — d=128, heads=4.
defaults:
  - ablation/training/aug_supcon_arch_base
  - _self_

tags: ["ablation", "backbone", "aug_supcon", "d128", "heads_4"]

model:
  n_heads: 4

logger:
  mlflow:
    run_name: "aug_supcon_arch_heads_4"
