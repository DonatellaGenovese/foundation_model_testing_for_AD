# @package _global_
# selfsupcon scheduler=none ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_sched_cern
  - _self_

tags: ["ablation", "sched", "selfsupcon", "d128", "sched_none"]

model:
  scheduler: null

logger:
  mlflow:
    experiment_name: "ablation_sched"
    run_name: "selfsupcon_sched_none"
