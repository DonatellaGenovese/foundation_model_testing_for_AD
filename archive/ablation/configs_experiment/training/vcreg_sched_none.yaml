# @package _global_
# vcreg scheduler=none ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vcreg
  - override /paths: ablation_sched_cern
  - _self_

tags: ["ablation", "sched", "vcreg", "d128", "sched_none"]

model:
  scheduler: null

logger:
  mlflow:
    experiment_name: "ablation_sched"
    run_name: "vcreg_sched_none"
