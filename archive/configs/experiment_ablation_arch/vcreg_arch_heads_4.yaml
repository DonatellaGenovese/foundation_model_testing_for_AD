# @package _global_
# VCREG backbone ablation — d=128, heads=4.
defaults:
  - ablation/training/vcreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vcreg", "d128", "heads_4"]

model:
  n_heads: 4

logger:
  mlflow:
    run_name: "vcreg_arch_heads_4"
