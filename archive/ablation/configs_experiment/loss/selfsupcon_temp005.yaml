# @package _global_
# SelfSupCon loss ablation — temperature=0.05
defaults:
  - ablation/loss/backbone_selfsupcon
  - _self_

tags: ["ablation", "loss", "selfsupcon", "temp005"]
model:
  temperature: 0.05
  base_temperature: 0.05
logger:
  mlflow:
    run_name: "selfsupcon_temp005"
