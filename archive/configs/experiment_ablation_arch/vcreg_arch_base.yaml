# @package _global_
# VCREG backbone architecture ablation — best d=128 configuration.
defaults:
  - ablation/training/backbone_vcreg
  - override /paths: ablation_backbone_cern
  - _self_

tags: ["ablation", "backbone", "vcreg", "d128", "100k"]

model:
  d_model: 128
  n_heads: 8
  num_layers: 4
  d_ff: 1024
  dropout: 0.1

logger:
  mlflow:
    experiment_name: "ablation_backbone"
    run_name: "vcreg_arch_base"
