# @package _global_
# AugSupCon loss ablation — temperature=0.15
defaults:
  - ablation/loss/backbone_aug_supcon
  - _self_

tags: ["ablation", "loss", "aug_supcon", "temp015"]
model:
  temperature: 0.15
  base_temperature: 0.15
logger:
  mlflow:
    run_name: "aug_supcon_temp015"
