# @package _global_
# aug_supcon scheduler=none ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_aug_supcon
  - override /paths: ablation_sched_cern
  - _self_

tags: ["ablation", "sched", "aug_supcon", "d128", "sched_none"]

model:
  scheduler: null

logger:
  mlflow:
    experiment_name: "ablation_sched"
    run_name: "aug_supcon_sched_none"
