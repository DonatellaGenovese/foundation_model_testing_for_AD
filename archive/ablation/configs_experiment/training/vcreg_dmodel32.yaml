# @package _global_
# vcreg d_model=32 ablation
defaults:
  - ablation/training/backbone_vcreg
  - _self_

tags: ["ablation", "training", "vcreg", "dmodel32", "100k"]
model:
  d_model: 32
  n_heads: 4
  d_ff: 128

logger:
  mlflow:
    run_name: "vcreg_dmodel32"
