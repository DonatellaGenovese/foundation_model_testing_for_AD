# @package _global_
# SimCLR (SelfSupCon) — d_model=128, OLD arch scale (n_heads=8, d_ff=1024, hidden=128)
defaults:
  - backbone/backbone_selfsupcon
  - new_exp/_final_simclr
  - _self_

tags: ["new_exp", "simclr", "dmodel128", "cern", "old_hparams"]

model:
  d_model: 128
  n_heads: 8
  d_ff: 1024
  hidden_projection_dim: 128

logger:
  mlflow:
    run_name: "simclr_dmodel128"
