# @package _global_
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_nheads_cern
  - _self_

tags: ["ablation", "nheads", "selfsupcon", "d128", "nheads_2"]

model:
  n_heads: 2

logger:
  mlflow:
    experiment_name: "ablation_nheads"
    run_name: "selfsupcon_nheads_2"
