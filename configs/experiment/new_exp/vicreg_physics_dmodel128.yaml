# @package _global_
# VICReg (physics aug, layers=4, accumulate=8) production run — d_model=128
defaults:
  - backbone/backbone_vicreg
  - new_exp/_final_vicreg
  - _self_

tags: ["new_exp", "vicreg", "physics", "dmodel128", "cern"]

model:
  d_model: 128
  projection_dim: 64   # d/2 (paper value)

logger:
  mlflow:
    run_name: "vicreg_physics_dmodel128"
