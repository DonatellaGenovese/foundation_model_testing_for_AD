# @package _global_
# AugSupCon — physics augmentation (phi rotation + eta boost + pT smearing)
# Usage: python src/train.py experiment=ablation/aug_supcon_physics seed=42

defaults:
  - ablation/backbone_aug_supcon
  - _self_

tags: ["ablation", "augmentation", "aug_supcon", "physics"]

model:
  augmentation_type: "physics"
  eta_sigma: 1.5
  pt_sigma: 0.1

logger:
  mlflow:
    run_name: "ablation_aug_supcon_physics"
