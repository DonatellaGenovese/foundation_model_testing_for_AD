# @package _global_
# VCReg loss ablation — var_weight=25 (baseline)
defaults:
  - ablation/loss/backbone_vcreg
  - _self_

tags: ["ablation", "loss", "vcreg", "var25", "baseline"]
model:
  var_weight: 25.0
  cov_weight: 1.0
