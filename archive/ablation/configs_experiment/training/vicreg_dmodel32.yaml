# @package _global_
# vicreg d_model=32 ablation — random particle augmentation
defaults:
  - ablation/training/backbone_vicreg
  - _self_

tags: ["ablation", "training", "vicreg", "dmodel32", "100k"]
model:
  d_model: 32
  n_heads: 4
  d_ff: 128
  projection_dim: 16
  hidden_projection_dim: 64

logger:
  mlflow:
    run_name: "vicreg_dmodel32"
