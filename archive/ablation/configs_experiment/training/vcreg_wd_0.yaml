# @package _global_
# vcreg weight_decay=0 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vcreg
  - override /paths: ablation_wd_cern
  - _self_

tags: ["ablation", "wd", "vcreg", "d128", "wd_0"]

model:
  optimizer:
    weight_decay: 0.0

logger:
  mlflow:
    experiment_name: "ablation_wd"
    run_name: "vcreg_wd_0"
