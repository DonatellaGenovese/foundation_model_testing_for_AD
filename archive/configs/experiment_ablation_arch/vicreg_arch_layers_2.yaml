# @package _global_
# VICREG backbone ablation — d=128, layers=2.
defaults:
  - ablation/training/vicreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vicreg", "d128", "layers_2"]

model:
  num_layers: 2

logger:
  mlflow:
    run_name: "vicreg_arch_layers_2"
