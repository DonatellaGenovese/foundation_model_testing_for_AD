# @package _global_
# SELFSUPCON backbone ablation — d=128, dff=512.
defaults:
  - ablation/training/selfsupcon_arch_base
  - _self_

tags: ["ablation", "backbone", "selfsupcon", "d128", "dff_512"]

model:
  d_ff: 512

logger:
  mlflow:
    run_name: "selfsupcon_arch_dff_512"
