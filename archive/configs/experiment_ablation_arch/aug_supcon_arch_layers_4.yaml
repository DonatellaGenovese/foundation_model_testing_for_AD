# @package _global_
# AUG_SUPCON backbone ablation — d=128, layers=4.
defaults:
  - ablation/training/aug_supcon_arch_base
  - _self_

tags: ["ablation", "backbone", "aug_supcon", "d128", "layers_4"]

model:
  num_layers: 4

logger:
  mlflow:
    run_name: "aug_supcon_arch_layers_4"
