# @package _global_
# vicreg weight_decay=0.01 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vicreg
  - override /paths: ablation_wd_cern
  - _self_

tags: ["ablation", "wd", "vicreg", "d128", "wd_1e2"]

model:
  optimizer:
    weight_decay: 0.01

logger:
  mlflow:
    experiment_name: "ablation_wd"
    run_name: "vicreg_wd_1e2"
