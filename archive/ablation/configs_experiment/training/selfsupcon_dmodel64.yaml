# @package _global_
# selfsupcon d_model=64 ablation
defaults:
  - ablation/training/backbone_selfsupcon
  - _self_

tags: ["ablation", "training", "selfsupcon", "dmodel64", "100k"]
model:
  d_model: 64
  n_heads: 8
  d_ff: 256
  projection_dim: 32
  hidden_projection_dim: 128
logger:
  mlflow:
    run_name: "selfsupcon_dmodel64"
