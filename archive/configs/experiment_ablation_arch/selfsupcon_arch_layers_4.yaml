# @package _global_
# SELFSUPCON backbone ablation — d=128, layers=4.
defaults:
  - ablation/training/selfsupcon_arch_base
  - _self_

tags: ["ablation", "backbone", "selfsupcon", "d128", "layers_4"]

model:
  num_layers: 4

logger:
  mlflow:
    run_name: "selfsupcon_arch_layers_4"
