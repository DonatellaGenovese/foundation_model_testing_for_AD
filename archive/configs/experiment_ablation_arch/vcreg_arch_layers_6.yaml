# @package _global_
# VCREG backbone ablation — d=128, layers=6.
defaults:
  - ablation/training/vcreg_arch_base
  - _self_

tags: ["ablation", "backbone", "vcreg", "d128", "layers_6"]

model:
  num_layers: 6

logger:
  mlflow:
    run_name: "vcreg_arch_layers_6"
