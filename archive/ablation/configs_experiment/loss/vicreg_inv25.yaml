# @package _global_
# VICReg loss ablation — inv_weight=25
defaults:
  - ablation/loss/backbone_vicreg_physics
  - _self_

tags: ["ablation", "loss", "vicreg", "inv25"]
model:
  inv_weight: 25.0
logger:
  mlflow:
    run_name: "vicreg_inv25"
