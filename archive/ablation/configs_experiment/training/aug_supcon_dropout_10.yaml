# @package _global_
# AugSupCon dropout=0.1 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_aug_supcon
  - override /paths: ablation_dropout_cern
  - _self_

tags: ["ablation", "dropout", "aug_supcon", "d128", "dropout_10"]

model:
  dropout: 0.1

logger:
  mlflow:
    experiment_name: "ablation_dropout"
    run_name: "aug_supcon_dropout_10"
