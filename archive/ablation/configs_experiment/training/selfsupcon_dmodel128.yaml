# @package _global_
# selfsupcon d_model=128 ablation
defaults:
  - ablation/training/backbone_selfsupcon
  - _self_

tags: ["ablation", "training", "selfsupcon", "dmodel128", "100k"]
model:
  d_model: 128
  n_heads: 8
  d_ff: 512
  projection_dim: 64
  hidden_projection_dim: 256
logger:
  mlflow:
    run_name: "selfsupcon_dmodel128"
