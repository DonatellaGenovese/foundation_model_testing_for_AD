# @package _global_
# selfsupcon lr=0.0005 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_selfsupcon
  - override /paths: ablation_lr_cern
  - _self_

tags: ["ablation", "lr", "selfsupcon", "d128", "lr_5e4"]

model:
  optimizer:
    lr: 0.0005

logger:
  mlflow:
    experiment_name: "ablation_lr"
    run_name: "selfsupcon_lr_5e4"
