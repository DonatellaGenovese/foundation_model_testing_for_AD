# @package _global_
# SelfSupCon — physics augmentation (phi rotation + eta boost + pT smearing)
# Usage: python src/train.py experiment=ablation/selfsupcon_physics seed=42

defaults:
  - ablation/backbone_selfsupcon
  - _self_

tags: ["ablation", "augmentation", "selfsupcon", "physics"]

model:
  augmentation_type: "physics"
  eta_sigma: 1.5
  pt_sigma: 0.1

logger:
  mlflow:
    run_name: "ablation_selfsupcon_physics"
