# @package _global_
# vicreg batch_size=2048 ablation — standard d=128 architecture.
defaults:
  - ablation/training/backbone_vicreg
  - override /paths: ablation_bs_cern
  - _self_

tags: ["ablation", "bs", "vicreg", "d128", "bs_2048"]

data:
  batch_size: 2048

logger:
  mlflow:
    experiment_name: "ablation_bs"
    run_name: "vicreg_bs_2048"
