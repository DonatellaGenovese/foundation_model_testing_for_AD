# @package _global_
# selfsupcon d_model=32 ablation
defaults:
  - ablation/training/backbone_selfsupcon
  - _self_

tags: ["ablation", "training", "selfsupcon", "dmodel32", "100k"]
model:
  d_model: 32
  n_heads: 4
  d_ff: 128
  projection_dim: 16
  hidden_projection_dim: 64
logger:
  mlflow:
    run_name: "selfsupcon_dmodel32"
