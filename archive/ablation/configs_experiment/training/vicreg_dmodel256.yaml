# @package _global_
# vicreg d_model=256 ablation — random particle augmentation
defaults:
  - ablation/training/backbone_vicreg
  - _self_

tags: ["ablation", "training", "vicreg", "dmodel256", "100k"]
model:
  d_model: 256
  n_heads: 8
  d_ff: 1024
  projection_dim: 128
  hidden_projection_dim: 512

logger:
  mlflow:
    run_name: "vicreg_dmodel256"
